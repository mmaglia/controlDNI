{% extends 'base.html.twig' %}

{% block title %}ListaControl{% endblock %}

{% block body %}
    <h1>ListaControl</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lista_control.id }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ lista_control.fecha ? lista_control.fecha|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ lista_control.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('lista_control_index') }}">back to list</a>

    <a href="{{ path('lista_control_edit', {'id': lista_control.id}) }}">edit</a>

    {{ include('lista_control/_delete_form.html.twig') }}
{% endblock %}
